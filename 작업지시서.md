# 작업지시서 — 유통기한 임박재고 분석 도구

## 1. 배경 요약

매달 두 개의 엑셀 파일을 대조해서 웰릿·클리너리 브랜드의 유통기한 임박 재고를 파악하고, 품목별로 "품절/단종 처리" 또는 "최상위 옵션 삭제"를 결정하는 작업을 자동화한다.

---

## 2. 입력 파일 스펙

### 파일1 — 신재고입출고
```
파일명 패턴: YYMM_신재고입출고_그외팀사용.xlsb
예시: 2606_신재고입출고_그외팀사용.xlsb
형식: Excel Binary Workbook (.xlsb)
사용 시트: 유통기한관리
```

**유통기한관리 시트 구조:**
- 0~3행: 타이틀/설명 행 (건너뜀)
- 4행 (0-indexed): 헤더
- 5행~ (0-indexed): 실제 데이터

| 0-indexed 열번호 | 헤더명 | 비고 |
|----------------|--------|------|
| 7 | 상품코드 | 조인 키 |
| 8 | 품명 | '웰릿' 또는 '클리너리' 시작 필터 |
| 13 | 유통기한 | 엑셀 시리얼 숫자 → 날짜 변환 필요 |
| 14 | 최종재고수량 | 실제 재고 (단위: 개) |

**날짜 변환 공식:**
```python
from datetime import datetime, timedelta
date = datetime(1899, 12, 30) + timedelta(days=int(시리얼숫자))
```

### 파일2 — 유통기한 임박재고 공유용
```
파일명 패턴: YY년 MM월 기준 유통기한 임박재고_공유용.xlsx
예시: 26년 6월 기준 유통기한 임박재고_공유용.xlsx
형식: Excel Workbook (.xlsx)
사용 시트: 상품별수불현황
```

**상품별수불현황 시트 구조:**
- 1행 (0-indexed 0): 헤더
- 2행~ (0-indexed 1~): 실제 데이터

| 0-indexed 열번호 | 헤더명 | 비고 |
|----------------|--------|------|
| 1 | 구분 | 카테고리 (병합 셀, 아래 행에는 None) |
| 2 | 상품코드 | 조인 키 |
| 3 | 상품명 | 짧은 이름 (브랜드명 없음) |
| 4 | 소비기한 | 문자열 YYYY-MM-DD |
| 5 | 재고 | 참고용 (정확도 낮음) |
| 6 | 유통기한 임박 | 7~9개월 / 4~6개월 / 3개월 |

**구분 컬럼 주의사항:**
- 병합 셀이라 첫 행만 값이 있고 나머지는 None
- `\n`으로 구분된 긴 텍스트이므로 첫 줄만 파싱
- 카테고리 파싱 예: `'7-9개월\n\n[조치사항]\n...'` → `'7-9개월'`

---

## 3. 처리 로직 상세

### Step 1. 파일1에서 웰릿/클리너리 추출
```
조건: 품명(index 8)이 '웰릿' 또는 '클리너리'로 시작
추출: 상품코드, 품명, 유통기한(변환), 최종재고수량
```

### Step 2. 파일2에서 카테고리 추출
```
조건: 없음 (전체)
추출: 상품코드, 카테고리(구분 첫 줄)
중복 상품코드: 처음 등장한 카테고리 사용
```

### Step 3. 상품코드로 조인
```
파일1 상품코드 ∩ 파일2 상품코드 → 교집합만 처리
파일1에만 있는 것: 제외 (아직 임박 아님)
파일2에만 있는 것: 제외 (웰릿/클리너리 아님)
```

### Step 4. 상품코드별 집계
```
같은 상품코드의 모든 로트를 소비기한 오름차순 정렬
최단 소비기한 = lots[0].expiry
총 재고 = sum(lot.stock for lot in lots)  ← 파일1 기준
액션 날짜 = 최단 소비기한 - 4개월
```

### Step 5. 긴급도 판별
```
오늘 날짜 기준:
  액션 날짜 ≤ 오늘           → 🔴 즉시 조치
  오늘 < 액션 날짜 ≤ 오늘+1달 → 🟡 이번 달 조치
  오늘+1달 < 액션 날짜        → 🟢 예정
```

---

## 4. 조치 결정 기준표

| 파일2 카테고리 | 남은 기간 | 조치 내용 | 메시지 예시 |
|-------------|----------|----------|-----------|
| 3개월 | ~3개월 | 품절/단종 처리 | "유통기한 임박으로 금일부터 출고 중단 부탁드립니다!" |
| 4-6개월 | 4~6개월 | 최상위 옵션 삭제 | 해당 상품 세트 옵션 직접 삭제 |
| 7-9개월 | 7~9개월 | 최상위 옵션 삭제 | 해당 상품 세트 옵션 직접 삭제 |

> **소비자 여유 원칙:** 실제 소비기한보다 4개월 전에 조치 (소비자가 구매 후 사용할 1개월 여유 확보)
> **다중 로트 원칙:** 같은 상품코드의 짧은 로트 기준으로 판단, 긴 로트도 함께 조치

---

## 5. 출력 리포트 스펙

### 화면 구성 (Streamlit)
1. **요약 카드:** 총 대상 / 즉시 조치 / 이번 달 조치 / 예정
2. **카테고리별 섹션:** 3개월 → 4-6개월 → 7-9개월 순
3. **상품별 expander:** 즉시 조치 항목은 기본 열림
4. **내부 정보:** 최단 소비기한, 남은 기간, 총 재고, 액션 날짜, 조치 내용, 로트 상세

### 다운로드 (Excel)
| 컬럼 | 내용 |
|------|------|
| 긴급도 | 🔴/🟡/🟢 |
| 구분 | 카테고리 |
| 브랜드 | 웰릿 / 클리너리 |
| 상품코드 | - |
| 품명 | 파일1 전체명 |
| 최단 소비기한 | YYYY-MM-DD |
| 남은 기간(개월) | 정수 |
| 총 재고(개) | 정수 |
| 액션 날짜 | YYYY-MM-DD |
| 조치사항 | 텍스트 |

---

## 6. 월간 운영 체크리스트

```
[ ] 1. 이번 달 신재고입출고 xlsb 파일 준비 (YYMM_신재고입출고_그외팀사용.xlsb)
[ ] 2. 이번 달 임박재고 공유용 xlsx 파일 준비 (YY년 MM월 기준 유통기한 임박재고_공유용.xlsx)
[ ] 3. 앱 실행: streamlit run app.py
[ ] 4. 파일1 업로드 → 파일2 업로드
[ ] 5. 기준일 확인 (오늘 날짜)
[ ] 6. 분석 실행 클릭
[ ] 7. 🔴 즉시 조치 항목 먼저 처리
[ ] 8. 리포트 엑셀 다운로드 후 보관
```

---

## 7. 알려진 데이터 이슈

| 이슈 | 설명 | 현재 처리 방식 |
|------|------|-------------|
| 재고 숫자 불일치 | 파일1과 파일2의 재고 수량이 다를 수 있음 | 파일1 최종재고수량을 기준으로 사용 |
| 브랜드명 없는 상품명 | 파일2의 상품명은 브랜드명(웰릿/클리너리) 없이 짧게 기재 | 파일1 품명을 그대로 표시 |
| 다중 로트 카테고리 | 파일2에서 짧은 로트 기준 카테고리에 긴 로트도 함께 표시됨 | 의도된 동작 (함께 출고 중단) |
| xlsb 유통기한 없는 행 | 유통기한 컬럼이 None인 경우 (비소비기한 제품 등) | 해당 로트 제외, 다른 로트로 판단 |

---

## 8. 실행 환경

```bash
# 패키지 설치
pip install -r requirements.txt

# 앱 실행
cd expiry-stock-manager
streamlit run app.py
```

**요구 패키지:**
- streamlit
- pandas
- openpyxl
- pyxlsb
- python-dateutil
