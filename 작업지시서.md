# 유통기한 임박재고 관리 도구 — 작업지시서

> 담당: 오희수 (hs.oh@2359.co.kr)  
> 브랜드: 웰릿 / 클리너리  
> 배포: Streamlit Community Cloud (`heesuclara/expiry-stock-manager`)

---

## 1. 프로젝트 개요

매월 재사용하는 유통기한 임박재고 분석 도구.  
두 개의 엑셀 파일을 업로드하면 브랜드별로 긴급도를 분류하고, 조치 날짜·입고예정 여부·재판매방지 SKU를 자동으로 확인해준다.

---

## 2. 입력 파일

| 파일 | 형식 | 용도 |
|------|------|------|
| `YYMM_신재고입출고_그외팀사용.xlsb` | `.xlsb` | 팀/브랜드/상품코드 매핑, 로트별 재고·유통기한, 입고예정(구매) |
| `YY년 MM월 기준 유통기한 임박재고_공유용.xlsx` | `.xlsx` | 카테고리(3개월/4-6개월/6개월 이상), 소비기한 보조 확인 |

### 파일1 사용 시트

| 시트 | 설명 |
|------|------|
| `재고수불종합` | 담당팀 / 스타일코드(브랜드) / 상품코드 매핑. 헤더 row3, 데이터 row4~ |
| `유통기한관리` | 상품코드별 로트 재고·유통기한. 헤더 row4, 데이터 row5~ |
| `입고예정(구매)` | 입고 예정 발주 잔량·예정일. 헤더 row2, 데이터 row3~ |

### 파일2 사용 시트

`상품별수불현황`이 포함된 시트명 자동 탐색 (예: `상품별수불현황_전사 공유용`).  
없으면 첫 번째 시트 사용.

---

## 3. 사이드바 선택 흐름

```
1차: 담당팀 선택 (필수) — 재고수불종합 시트의 담당팀 열
2차: 브랜드 선택 (필수, 다중) — 전체 파일의 모든 브랜드 표시
3차: 해외전용 SKU 포함 여부 (선택) — 해당 브랜드에 해외전용 코드 있을 때만 노출
```

---

## 4. 긴급도 분류

액션 날짜 = `최단 소비기한 - 4개월` (주말이면 직전 금요일로 조정)

| 등급 | 조건 | 색상 |
|------|------|------|
| 🔴 즉시 조치 | 액션 날짜 ≤ 오늘 | 빨강 |
| 🟡 이번 달 조치 | 액션 날짜 ≤ 오늘 + 1개월 | 노랑 |
| 🟠 2~3개월 내 조치 | 액션 날짜 ≤ 오늘 + 3개월 | 주황 |
| 🟢 추후 확인 | 그 외 | 초록 |

---

## 5. 구현된 기능

### 분석 페이지
- 전체 목록 테이블 (상단 배치)
- 브랜드 탭별 ✅ 체크리스트 (`data_editor` + `CheckboxColumn`)
- 완료 진행률 표시
- 분석 실행마다 체크 초기화 (`run_id` 키 사용)

### 입고예정 확인 (🔴🟡🟠 항목만)
`입고예정(구매)` 시트에서 상품코드별 발주잔량 합산

| 결과 | 체크리스트 | 추천 조치 |
|------|-----------|-----------|
| 발주잔량 > 0 | 📦 입고예정 | 품절처리 문의 |
| 발주잔량 없음 | ⛔ 미입고예정 | 단종처리 문의 |

### 재판매방지 SKU 확인
- `data/resale_prevention.json`에 35개 SKU 저장 (상품코드 키)
- 체크리스트에 ✌️ 표시, 상세 뷰에서 최소옵션·추천조치 안내

### 히스토리
- 분석 결과 월별 자동 저장 (`data/history.json`, 같은 달 중복 저장 방지)
- 월별 브랜드 탭 조회
- JSON 내보내기 / 가져오기

---

## 6. 파일 구조

```
expiry-stock-manager/
├── app.py                  # 메인 앱
├── requirements.txt
├── data/
│   ├── resale_prevention.json   # 재판매방지 SKU 목록 (GitHub 포함)
│   └── history.json             # 월별 히스토리 (GitHub 제외)
├── .streamlit/
│   └── config.toml
└── .gitignore
```

---

## 7. 재판매방지 JSON 구조

```json
{
  "상품코드": {
    "제품명": "...",
    "브랜드": "웰릿 또는 클리너리",
    "최소옵션": ["옵션1", "옵션2"],
    "업데이트일자": "YYYY.MM.DD",
    "비고": "(선택)"
  }
}
```

업데이트 시 `data/resale_prevention.json` 직접 수정 후 GitHub push.

---

## 8. 기술 스택

| 항목 | 내용 |
|------|------|
| 언어 | Python 3.x |
| 프레임워크 | Streamlit ≥ 1.35 |
| 엑셀 파싱 | `pyxlsb` (.xlsb), `openpyxl` (.xlsx) |
| 데이터 처리 | `pandas` |
| 날짜 계산 | `python-dateutil` (relativedelta) |
| 배포 | Streamlit Community Cloud |
| 소스 관리 | GitHub (`heesuclara/expiry-stock-manager`) |

---

## 9. 배포 업데이트 방법

코드 수정 후 GitHub push하면 Streamlit Cloud가 자동으로 재배포함.

```bash
git add app.py
git commit -m "수정 내용"
git push origin master
```

---

## 10. 미해결 / 추후 작업

- [ ] 재판매방지 JSON 노션 연동 자동 갱신 (현재는 수동 업데이트)
- [ ] 히스토리 Streamlit Cloud 영구 저장 문제 (파일시스템 리셋 시 소실 → 현재 JSON 내보내기로 대응)
- [ ] 파일2 칼럼 위치 변경 시 자동 감지 로직 보완
