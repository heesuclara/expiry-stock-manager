# PRD — 유통기한 임박재고 분석 도구

## 배경 및 문제 정의

웰릿·클리너리 브랜드 재고 중 유통기한이 임박한 상품은 매달 두 개의 엑셀 파일을 수동으로 대조해 조치를 결정하고 있다. 현재는 파일을 열어 눈으로 확인한 뒤 조치 사항을 직접 판단하는 방식이라 시간이 걸리고 놓치는 항목이 생길 수 있다.

**핵심 문제:** 두 파일을 손으로 대조 → 조치 기준 직접 계산 → 처리 항목 누락 위험

---

## 사용자

| 항목 | 내용 |
|------|------|
| 주 사용자 | 재고·상품 담당자 (비개발자) |
| 사용 빈도 | 월 1회 (파일 업데이트 후) |
| 사용 환경 | 웹 브라우저 (로컬 실행) |

---

## 목표

1. 파일 2개를 업로드하면 5초 안에 조치 리포트가 나온다
2. 조치 우선순위(즉시/이번 달/예정)가 색으로 구분된다
3. 리포트를 엑셀로 다운로드할 수 있다
4. 코딩 없이 매달 파일만 교체해서 재사용 가능하다

---

## 데이터 소스

### 파일1 — 신재고입출고 (`.xlsb`)
- 파일명 패턴: `YYMM_신재고입출고_그외팀사용.xlsb`
- 사용 시트: `유통기한관리`
- **재고 수량의 기준 파일** (파일2보다 정확)

| 컬럼 | 위치 | 설명 |
|------|------|------|
| 상품코드 | H열 (index 7) | 제품 고유 코드, 파일2와 조인 키 |
| 품명 | I열 (index 8) | 전체 상품명. '웰릿' 또는 '클리너리'로 시작하는 것만 추출 |
| 유통기한 | N열 (index 13) | 엑셀 시리얼 숫자 형식 → 날짜 변환 필요 |
| 최종재고수량 | O열 (index 14) | 로트별 재고 수량 (단위: 개) |

- 헤더 행: 5행 (0-indexed 4), 데이터 시작: 6행 (0-indexed 5)
- 같은 상품코드가 유통기한 로트별로 여러 행으로 존재

### 파일2 — 유통기한 임박재고 공유용 (`.xlsx`)
- 파일명 패턴: `YY년 MM월 기준 유통기한 임박재고_공유용.xlsx`
- 사용 시트: `상품별수불현황`
- **임박 카테고리 분류의 기준 파일**

| 컬럼 | 위치 | 설명 |
|------|------|------|
| 구분 | B열 (index 1) | 7-9개월 / 4-6개월 / 3개월 카테고리 |
| 상품코드 | C열 (index 2) | 파일1과 조인 키 |
| 상품명 | D열 (index 3) | 짧은 이름 (브랜드명 없음) |
| 소비기한 | E열 (index 4) | 문자열 날짜 (YYYY-MM-DD) |
| 재고 | F열 (index 5) | 참고용 (실제 재고는 파일1 기준) |

---

## 핵심 비즈니스 로직

### 1. 조인 방식
- 파일1의 `상품코드` ∩ 파일2의 `상품코드` 교집합만 처리
- 파일1 기준으로 '웰릿' 또는 '클리너리'로 시작하는 품명만 대상

### 2. 수출용 SKU 제외 _(v1.1 추가)_
- 수출용 상품은 분석 대상에서 제외
- 제외 키워드: `수출용`, `(대만)`, `(홍콩)`, `(싱가폴)`, `(중국)` 중 하나라도 품명에 포함되면 제외

### 3. 다중 로트 처리
- 같은 상품코드에 유통기한이 다른 로트가 여러 개 있을 수 있음
- **가장 짧은 유통기한 로트** 기준으로 카테고리와 액션 날짜 결정
- 총 재고 = 해당 상품코드의 모든 로트 재고 합산 (파일1 기준)

### 4. 최단 소비기한 결정 — 파일2 보조 확인 _(v1.1 추가)_
- 파일1(신재고입출고)에 등록되지 않은 로트가 파일2(임박재고)에 있을 수 있음
  - 예: 이미 폐기 처리 중이거나 별도 창고에 있는 로트
- **최단 소비기한 = min(파일1 로트 날짜, 파일2 소비기한 날짜)**
- 파일2에만 있는 날짜가 더 이를 경우 해당 날짜를 기준으로 액션 날짜 계산
- 재고 수량은 파일1 기준 유지 (파일1이 더 정확)

### 5. 조치 기준표

| 카테고리 | 남은 기간 | 조치 내용 | 액션 날짜 계산 |
|----------|----------|----------|--------------|
| 3개월 | 3개월 이하 | 품절/단종 처리 | 최단 소비기한 - 4개월 |
| 4-6개월 | 4~6개월 | 최상위 옵션 삭제 (4개월 이상 세트) | 최단 소비기한 - 4개월 |
| 7-9개월 | 7~9개월 | 최상위 옵션 삭제 (6개월 이상 세트) | 최단 소비기한 - 4개월 |

> **소비자 여유 1개월 원칙:** 소비자가 구매 후 1개월 내 사용할 수 있도록, 실제 소비기한보다 4개월 전에 조치한다.

### 6. 액션 날짜 주말 조정 _(v1.1 추가)_
- 계산된 액션 날짜가 **토요일**이면 → **금요일**로 조정
- 계산된 액션 날짜가 **일요일**이면 → **금요일**로 조정 (2일 앞당김)
- 평일이면 그대로 유지

### 7. 긴급도 구분 _(v1.1: 2~3개월 구간 추가)_

| 긴급도 | 기준 | 표시 |
|--------|------|------|
| 🔴 즉시 조치 | 액션 날짜 ≤ 오늘 | 기본 펼침 |
| 🟡 이번 달 조치 | 오늘 < 액션 날짜 ≤ 오늘 + 1개월 | 기본 펼침 |
| 🟠 2~3개월 내 조치 | 오늘 + 1개월 < 액션 날짜 ≤ 오늘 + 3개월 | 접힘 |
| 🟢 추후 확인 | 액션 날짜 > 오늘 + 3개월 | 접힘, 하단 표시 |

> **표시 원칙:** 🔴🟡🟠 은 이번 분석 주기(3개월)에 처리가 필요한 항목. 🟢 추후 확인은 목록에 포함하되 하단에 별도 섹션으로 정리.

---

## MVP 기능 범위

### 포함
- [x] 파일2개 업로드 (xlsb + xlsx)
- [x] 웰릿/클리너리 자동 필터링
- [x] 수출용 SKU 자동 제외
- [x] 상품코드 기반 조인
- [x] 파일2 소비기한 보조 확인 (파일1 누락 로트 보완)
- [x] 긴급도별 색상 구분 리포트 (4단계: 즉시/이번달/2~3개월/추후확인)
- [x] 액션 날짜 주말 → 직전 금요일 자동 조정
- [x] 로트별 상세 표시
- [x] 엑셀 다운로드
- [x] 기준일 변경 가능

### 미포함 (v2 이후)
- [ ] 이메일/슬랙 자동 발송
- [ ] 월별 이력 저장
- [ ] 타 브랜드 확장
- [ ] 재고 소진 예상일 계산
- [ ] 클라우드 배포

---

## 성공 기준

- 파일 업로드 후 5초 이내 리포트 표시
- 수동 확인 시간 기존 대비 80% 단축
- 빠뜨린 항목 없이 전체 대상 표시
